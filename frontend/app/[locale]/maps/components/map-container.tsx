'use client';

// This is a placeholder component for the map container
// The actual map functionality is in MapComponent

export function MapContainer() {
  return (
    <div className="h-full w-full bg-muted/20 flex items-center justify-center">
      <p className="text-muted-foreground">Map container placeholder</p>
    </div>
  );
}
